# Plan: prj-7164-task-mugsincc

**Project:** `PRJ-6419`  
**Task ID:** `task-mugsincc`  
**Repo:** `prj-7164-task-mugsincc`  

## Summary

Декомпозирую задачу «Сделай скрипт для синхронизации папок» на 4 subtasks: репозиторий+CLI, ядро синхронизации, конфиг+логи+уведомления, документация+тесты. Профиль round-robin между 20razrab1 и 30razrab2.

## Subtasks

### 1. Подготовить репозиторий и CLI-каркас

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру репо (sync_folders.py, requirements.txt, README.md, .gitignore, директории tests/ и config/). Реализовать CLI на argparse с параметрами: --source, --target, --config, --dry-run, --verbose, --once, --interval, --log-file. --help работает корректно.
- **Test plan:** python sync_folders.py --help выводит все параметры; python -c 'import argparse' не падает; в репо есть sync_folders.py, requirements.txt, README.md, tests/, config/

### 2. Реализовать ядро синхронизации

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать функции: scan_directory(path), compare_trees(src, dst), copy_file(src,dst), remove_extra(dst, src), ensure_dir(path). Однонаправленная синхронизация src→dst: копирование новых/изменённых файлов (по mtime+size), удаление лишних файлов в dst, создание недостающих директорий. Режим dry-run не меняет ФС, только логирует намерения.
- **Test plan:** pytest tests/test_sync_core.py зелёный: создаётся временное дерево src, после sync оно совпадает с dst; dry-run не меняет dst; remove_extra удаляет файлы отсутствующие в src
- **Dependencies:** `sub-1`

### 3. Конфиг (YAML/JSON), логирование, режим демона

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить загрузку конфига (YAML или JSON) со списком пар source/target, exclude-паттернами (glob) и интервалом. Настроить модуль logging: формат timestamp/level/message, вывод в файл и консоль, ротация при необходимости. Реализовать режим демона: циклический sync с заданным interval; корректная обработка Ctrl-C/SIGTERM.
- **Test plan:** Создать config/sync.yaml с двумя парами папок — скрипт читает и применяет обе; exclude-маска *.tmp не копируется; SIGTERM завершает цикл без traceback; log-файл содержит timestamp и level
- **Dependencies:** `sub-2`

### 4. Документация, упаковка и интеграционные тесты

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README с примерами запуска (one-shot, dry-run, daemon, через config), описанием exclude-паттернов и формата лога. Добавить setup.py/pyproject.toml для установки через pip. Написать интеграционный тест end-to-end (временные директории, реальная синхронизация, проверка дерева после sync). Прогнать flake8/ruff, зафиксировать версию 1.0.0.
- **Test plan:** pytest tests/ проходит все тесты; flake8/ruff без ошибок на .py файлах; pip install . устанавливает пакет; запуск sync-folders --help работает; README содержит все 4 сценария запуска
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
